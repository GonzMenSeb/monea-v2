import { Stack } from 'expo-router';

export default function TransactionsLayout(): React.ReactElement {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
}
